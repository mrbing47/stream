<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />

		<script defer src="/socket.io/socket.io.js"></script>

		<link rel="stylesheet" href="/css/room.css" type="text/css" />
		<link rel="stylesheet" href="/css/global.css" />

		<link rel="shortcut icon" href="/favicon/favicon.ico" type="image/x-icon" />

		<script src="https://cdnjs.cloudflare.com/ajax/libs/clipboard.js/2.0.4/clipboard.min.js"></script>
		<script defer src="/js/cookieFn.js"></script>
		<script defer src="/js/room.js"></script>

		<script src="https://kit.fontawesome.com/8d8384e88c.js" crossorigin="anonymous"></script>
		<link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet" />

		<title><%= room.title %></title>
	</head>
	<body>
		<div class="video-container" id="<%= usertype %> ">
			<video id="video"
			<% if (usertype === "leader") { %> 
				controls 
			<% } %>>
				<source src="<%= video.src %>" type="video/mp4" id="video-source" />
			</video>
			<div class="video-details">
				<div id="video-title"><%= video.title %></div>
				<div id="video-time"><%= video.time %></div>
				<div id="video-size"><%= video.size %></div>
			</div>
		</div>
		<div class="chat-container">
			<div class="room-details">
				<div id="room-title"><%= room.title %></div>
				<div id="room-id"><i id="copy-id-btn" class="far fa-copy"></i><%= room.id %></div>
				<div class="room-viewers">
					<i class="fas fa-eye"></i>
					<div id="viewer-count">
						<% if (typeof room.viewers === "number") { %> <%= room.viewers %> <% } %>
					</div>
				</div>
			</div>

			<div id="message-container">
			</div>
			<div class="send-container">
				<input type="text" name="chat-msg" id="user-msg" placeholder="Enter a message" />
				<i class="fas fa-paper-plane" id="send-btn"></i>
			</div>
		</div>
	</body>
</html>
